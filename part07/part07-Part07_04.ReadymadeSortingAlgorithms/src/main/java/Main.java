
import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;

public class Main {

    public static void main(String[] args) {
        // insert test code here
        int[] array = {3, 1, 5, 99, 3, 12};
    }
    
    public static void sort(int[] array) { //Sorts an array of integers
        Arrays.sort(array);
    }
    
    public static void sort(String[] array) { //Sorts an array of strings
        Arrays.sort(array);
    }
    
    public static void sortIntegers(ArrayList<Integer> integers) { //Sorts a list of strings
        Collections.sort(integers);
    }
    
    public static void sortStrings(ArrayList<String> strings) {
        Collections.sort(strings);
    }
}
